export const scrollToSectionConfig = {
    linksSelector: '.navbar__link',
    openMenuBtn: '.navbar__burger',
    menuSelector: '.navbar__menu',
    activeBtnClass: 'navbar__burger-active',
    activeMenuClass: 'navbar__menu-opened'
};

export const sliderConfig = {
    container: '.facts__slider',
    wrapper: '.facts__slider-wrapper',
    field: '.facts__slider-inner',
    slide: '.facts__slide',
    nextArrow: '.facts__slider-next',
    prevArrow: '.facts__slider-prev',
    totalCounter: '#total',
    currentCounter: '#current'
}

export const toggleMenuConfig = {
    btnOpenSelector:'.navbar__burger',
    menuSelector: '.navbar__menu',
    closeBtnSelector: '.navbar__close',
    activeBtnOpenClass: 'navbar__burger-active', 
    activeMenuClass: 'navbar__menu-opened'
}

export const timerConfig = {
    id: '.promotion__timer', 
    deadline: '2024-12-31'
}